import { getAdminAuditLogs } from "@/features/admin/admin-service";
import { PageHeader } from "@/components/workspace/page-header";

export default async function AdminAuditLogsPage() {
  const result = await getAdminAuditLogs();
  return <div className="grid gap-6"><PageHeader eyebrow="Admin / Audit logs" title="Operational audit trail" description="Append-oriented events generated by server-side workflows." /><section className="overflow-x-auto rounded-tf-lg border border-border bg-card"><table className="w-full min-w-[900px] text-left text-sm"><thead className="border-b border-border text-xs text-muted-foreground"><tr>{["Time", "Action", "Entity", "Actor", "Details"].map((label) => <th key={label} className="px-5 py-3">{label}</th>)}</tr></thead><tbody>{result.rows.map((event) => <tr key={event.id} className="border-b border-border/70"><td className="px-5 py-4 text-muted-foreground">{new Date(event.created_at).toLocaleString()}</td><td className="px-5 py-4 font-medium">{event.action}</td><td className="px-5 py-4">{event.entity_type} / {event.entity_id ?? "-"}</td><td className="px-5 py-4 font-mono text-xs">{event.actor_id ?? "system"}</td><td className="max-w-md truncate px-5 py-4 text-xs text-muted-foreground">{JSON.stringify(event.metadata)}</td></tr>)}</tbody></table></section>{result.rows.length === 0 && <p className="text-sm text-muted-foreground">No audit events found</p>}</div>;
}