import { notFound } from "next/navigation";
import { getAdminAccount } from "@/features/admin/admin-service";
import { PageHeader } from "@/components/workspace/page-header";
import { SectionCard } from "@/components/workspace/section-card";
import { StatusBadge } from "@/components/workspace/status-badge";

export default async function AdminAccountDetailPage({ params }: { params: Promise<{ id: string }> }) {
  try { const account = await getAdminAccount((await params).id); if (!account) notFound(); return <div className="grid gap-6"><PageHeader eyebrow="Admin / Accounts" title={account.account_name} description={account.id} /><section className="grid gap-3 md:grid-cols-2"><SectionCard title="Lifecycle"><div className="grid gap-3 text-sm"><div className="flex justify-between"><span className="text-muted-foreground">Owner</span><span>{account.user_id}</span></div><div className="flex justify-between"><span className="text-muted-foreground">Purchase</span><span>{account.purchase_id}</span></div><div className="flex justify-between"><span className="text-muted-foreground">Challenge plan</span><span>{account.challenge_plan_id}</span></div><div className="flex justify-between"><span className="text-muted-foreground">Status</span><StatusBadge tone={account.status === "funded" || account.status === "passed" ? "success" : account.status === "failed" || account.status === "closed" ? "danger" : "warning"}>{account.status}</StatusBadge></div><div className="flex justify-between"><span className="text-muted-foreground">Phase</span><span>{account.phase}</span></div></div></SectionCard><SectionCard title="Provider data"><div className="grid gap-3 text-sm"><span>Platform: {account.platform}</span><span>Provider account: {account.provider_account_id ?? "Integration pending"}</span><span>Balance: {account.provider_account_id ? account.balance : "Data unavailable"}</span><span>Equity: {account.provider_account_id ? account.equity : "Data unavailable"}</span></div></SectionCard></section></div>; } catch { notFound(); }
}