import { getAdminPayouts } from "@/features/payouts/payout-service";
import { PageHeader } from "@/components/workspace/page-header";
import { StatusBadge } from "@/components/workspace/status-badge";

export default async function AdminPayoutsPage() {
  const payouts = await getAdminPayouts();
  return <div className="grid gap-6"><PageHeader eyebrow="Admin / Payouts" title="Payout review" description="Requests require server-side administrative transition and manual settlement when no provider is configured." /><section className="overflow-x-auto rounded-tf-lg border border-border bg-card"><table className="w-full min-w-[760px] text-left text-sm"><thead className="border-b border-border text-xs text-muted-foreground"><tr>{["Request", "User", "Account", "Amount", "Method", "Status"].map((label) => <th key={label} className="px-5 py-3">{label}</th>)}</tr></thead><tbody>{payouts.map((payout) => <tr key={payout.id} className="border-b border-border/70"><td className="px-5 py-4 font-mono text-xs">{payout.id}</td><td className="px-5 py-4 font-mono text-xs">{payout.user_id}</td><td className="px-5 py-4 font-mono text-xs">{payout.account_id}</td><td className="px-5 py-4">${Number(payout.requested_amount).toFixed(2)}</td><td className="px-5 py-4">{payout.method}</td><td className="px-5 py-4"><StatusBadge tone={payout.status === "paid" ? "success" : payout.status === "rejected" ? "danger" : "warning"}>{payout.status}</StatusBadge></td></tr>)}</tbody></table></section></div>;
}