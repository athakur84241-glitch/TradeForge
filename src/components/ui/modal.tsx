"use client";
import * as Dialog from "@radix-ui/react-dialog";
import { AlertTriangle, CheckCircle2, Trash2, X } from "lucide-react";
import { Button } from "./button";
type ModalKind = "confirmation" | "success" | "delete" | "warning";
const settings: Record<ModalKind, { icon: typeof CheckCircle2; color: string; title: string; description: string; action: string; variant: "primary" | "danger" }> = { confirmation: { icon: CheckCircle2, color: "text-primary", title: "Confirm action", description: "Are you sure you want to continue? This action will be processed immediately.", action: "Confirm", variant: "primary" }, success: { icon: CheckCircle2, color: "text-success", title: "Successfully completed", description: "Your changes have been saved and are now active.", action: "Done", variant: "primary" }, delete: { icon: Trash2, color: "text-danger", title: "Delete this item?", description: "This cannot be undone. The selected item will be permanently removed.", action: "Delete", variant: "danger" }, warning: { icon: AlertTriangle, color: "text-warning", title: "Please review", description: "This action may affect your account configuration.", action: "Continue", variant: "primary" } };
export function ActionModal({ kind, trigger }: { kind: ModalKind; trigger: React.ReactNode }) { const item = settings[kind]; const Icon = item.icon; return <Dialog.Root><Dialog.Trigger asChild>{trigger}</Dialog.Trigger><Dialog.Portal><Dialog.Overlay className="fixed inset-0 z-50 bg-black/60 backdrop-blur-sm data-[state=open]:animate-in" /><Dialog.Content className="fixed left-1/2 top-1/2 z-50 w-[calc(100%-2rem)] max-w-md -translate-x-1/2 -translate-y-1/2 rounded-tf-xl border border-border bg-card p-6 shadow-2xl"><Dialog.Close className="absolute right-4 top-4 text-muted-foreground hover:text-foreground" aria-label="Close"><X className="size-4" /></Dialog.Close><Icon className={`size-9 ${item.color}`} /><Dialog.Title className="mt-4 font-display text-xl font-semibold">{item.title}</Dialog.Title><Dialog.Description className="mt-2 text-sm leading-6 text-muted-foreground">{item.description}</Dialog.Description><div className="mt-6 flex justify-end gap-3"><Dialog.Close asChild><Button variant="ghost">Cancel</Button></Dialog.Close><Dialog.Close asChild><Button variant={item.variant}>{item.action}</Button></Dialog.Close></div></Dialog.Content></Dialog.Portal></Dialog.Root>; }
